# Лабораторна робота № 11. Використання pandas для обробки табличних даних

**Мета:** здобути практичні навички роботи з бібліотекою pandas, яка є потужним інструментом для обробки та аналізу табличних даних у мові програмування Python. Навчитися завантажувати дані з файлів, проводити первинний аналіз даних, виконувати фільтрацію, сортування, групування, агрегування даних, а також додавати та видаляти стовпці й рядки. Навчитися обробляти пропущені значення та створювати зведені таблиці для підсумкового аналізу даних.

## Завдання для всіх варіантів

1.	Завантажити табличні дані з файлу [`data/sales_data.csv`](data/sales_data.csv) у DataFrame.
2.	Провести первинний аналіз даних: вивести перші та останні рядки, отримати загальну інформацію про DataFrame (кількість рядків, стовпців, типи даних), а також отримати статистичні характеристики числових стовпців (середнє, медіана, стандартне відхилення тощо).
3.	Відфільтрувати дані за певною умовою, наприклад, вибрати рядки, де значення в певному стовпці відповідає заданому критерію.
4.	Відсортувати дані за значеннями одного або декількох стовпців у порядку зростання або спадання.
5.	Згрупувати дані за одним або декількома стовпцями та обчислити агреговані значення (суму, середнє, кількість тощо) для кожної групи.
6.	Додати новий стовпець до DataFrame, значення якого обчислюються на основі значень інших стовпців.
7.	Видалити певні стовпці або рядки з DataFrame, які не є необхідними для подальшого аналізу.
8.	Обробити пропущені значення (NaN) у DataFrame, наприклад, замінити їх на середнє значення, медіану або видалити рядки з пропущеними значеннями.
9.	Створити зведену таблицю (pivot table) для підсумкового аналізу даних, де в рядках і стовпцях будуть вказані певні категорії, а в комірках – агреговані значення.

## Хід роботи

Студенту необхідно виконати завдання згідно зі своїм варіантом. Весь код для виконання лабораторної роботи необхідно писати у файлі [`src/lab11.py`](src/lab11.py). Результати роботи (виведення програми, відповіді на контрольні запитання) необхідно оформити у файлі [`REPORT.md`](REPORT.md).

### Приклад виконання завдання

Розглянемо приклад виконання лабораторної роботи на основі даних про продажі товарів у різних магазинах. Файл [`data/sales_data.csv`](data/sales_data.csv) містить наступні дані:
*   `order_id`: унікальний ідентифікатор замовлення.
*   `date`: дата здійснення замовлення.
*   `store_id`: ідентифікатор магазину, в якому було зроблено замовлення.
*   `product_id`: ідентифікатор товару.
*   `quantity`: кількість одиниць товару, проданих у даному замовленні.
*   `price`: ціна за одиницю товару.

Необхідно виконати наступні завдання:
1.	Завантажити дані з файлу [`data/sales_data.csv`](data/sales_data.csv)` у DataFrame.
2.	Вивести перші 5 рядків DataFrame, отримати загальну інформацію про нього та статистичні характеристики числових стовпців.
3.	Вибрати рядки, де `store_id` дорівнює 1 і `quantity` більше 2.
4.	Відсортувати дані за стовпцем `date` у порядку зростання, а потім за стовпцем `order_id` у порядку спадання.
5.	Згрупувати дані за `store_id` та `product_id` і обчислити сумарну кількість проданих товарів (`quantity`) для кожної групи.
6.	Додати стовпець `total_amount`, який містить загальну вартість кожного замовлення (добуток `quantity` та `price`).
7.	Видалити стовпці `order_id` та `product_id`.
8.	Замінити пропущені значення в стовпці `price` на середнє значення ціни для відповідного `store_id`.
9.	Створити зведену таблицю, де в рядках буде `store_id`, у стовпцях – місяці, витягнуті з дати замовлення (`date`), а в комірках – сумарна вартість продажів (`total_amount`) для кожного магазину та місяця.

## Завдання для виконання лабораторної роботи

### Для всіх варіантів
1.	Завантажити дані з файлу [`data/sales_data.csv`](data/sales_data.csv) у DataFrame.
2.	Вивести перші 5 рядків DataFrame, отримати загальну інформацію про нього та статистичні характеристики числових стовпців.

---

### Варіант 1
1.	Вибрати рядки, де `store_id` дорівнює 2 і `quantity` менше 3.
2.	Відсортувати дані за стовпцем `quantity` у порядку спадання.
3.	Згрупувати дані за `product_id` та обчислити середню ціну (`price`) для кожного товару.
4.	Додати стовпець `total_cost`, який містить загальну вартість кожного замовлення (добуток `quantity` та `price`).
5.	Видалити стовпець `date`.
6.	Замінити пропущені значення в стовпці `quantity` на 0.
7.	Створити зведену таблицю, де в рядках буде `store_id`, у стовпцях – `product_id`, а в комірках – сумарна кількість проданих товарів (`quantity`).

**Контрольне запитання:** Як знайти кількість унікальних значень у стовпці DataFrame?

---

### Варіант 2
1.	Вибрати рядки, де `date` знаходиться в діапазоні між '2024-03-01' та '2024-03-31'.
2.	Відсортувати дані за стовпцями `store_id` (зростання) та `date` (спадання).
3.	Згрупувати дані за `store_id` та обчислити сумарну вартість продажів (`quantity` * `price`) для кожного магазину.
4.	Додати стовпець `discount`, де для замовлень з `quantity` більше 5 буде значення 0.1 (знижка 10%), інакше 0.
5.	Видалити рядки, де `price` дорівнює 0.
6.	Заповнити пропущені значення в стовпці `price` медіанним значенням.
7.	Створити зведену таблицю, де в рядках буде `product_id`, у стовпцях – місяці, витягнуті з `date`, а в комірках – середня ціна (`price`) товару в кожному місяці.

**Контрольне запитання:** Як змінити індекси рядків DataFrame?

---

### Варіант 3
1.	Вибрати рядки, де `product_id` дорівнює 5 або 10.
2.	Відсортувати дані за `order_id` у порядку зростання.
3.	Згрупувати дані за `date` та `store_id` і обчислити кількість унікальних товарів (`product_id`), проданих в кожному магазині кожного дня.
4.	Додати стовпець `price_category`, де для товарів з ціною (`price`) менше 50 буде значення 'low', від 50 до 100 – 'medium', більше 100 – 'high'.
5.	Видалити стовпці `store_id` та `product_id`.
6.	Видалити рядки з пропущеними значеннями в будь-якому стовпці.
7.	Створити зведену таблицю, де в рядках буде `date`, а в стовпцях – `price_category`, а в комірках – максимальна кількість (`quantity`) проданих товарів у кожній категорії.

**Контрольне запитання:** Як перейменувати стовпець у DataFrame?

---

### Варіант 4
1.	Вибрати рядки, де `quantity` більше середнього значення `quantity` по всьому DataFrame.
2.	Відсортувати дані за `date` та `price` у порядку спадання.
3.	Згрупувати дані за `store_id` та `product_id` і обчислити мінімальну та максимальну ціну (`price`) для кожного товару в кожному магазині.
4.	Додати стовпець `day_of_week`, який містить день тижня, витягнутий з `date`.
5.	Видалити всі рядки, де `store_id` дорівнює 3.
6.	Замінити пропущені значення в `price` на середнє значення ціни для відповідного `product_id`.
7.	Створити зведену таблицю, де в рядках буде `store_id`, у стовпцях – дні тижня (`day_of_week`), а в комірках – середнє значення `quantity` для кожного магазину в кожен день тижня.

**Контрольне запитання:** Як створити новий стовпець у DataFrame на основі умов, що застосовуються до інших стовпців?

---

### Варіант 5
1.	Вибрати рядки, де `date` припадає на вихідні дні (субота або неділя).
2.	Відсортувати дані за `product_id` (зростання) та `quantity` (спадання).
3.	Згрупувати дані за `date` і обчислити загальну кількість (`quantity`) проданих товарів кожного дня.
4.	Додати стовпець `revenue`, який містить добуток `quantity` та `price`.
5.	Видалити дублікати рядків, залишаючи тільки перший з них.
6.	Заповнити пропущені значення в `store_id` найбільш часто зустрічаючимся значенням.
7.	Створити зведену таблицю, де в рядках буде `product_id`, у стовпцях – `store_id`, а в комірках – максимальна ціна (`price`) для кожного товару в кожному магазині.

**Контрольне запитання:** Як видалити дублікати рядків у DataFrame?

---

### Варіант 6
1.	Вибрати рядки, де `price` знаходиться в діапазоні між першим та третім квартилями (25% та 75%) значень `price` по всьому DataFrame.
2.	Відсортувати дані за `total_amount` (добуток `quantity` на `price`) у порядку спадання.
3.	Згрупувати дані за `product_id` та обчислити стандартне відхилення `price` для кожного товару.
4.	Додати стовпець `month`, який містить місяць, витягнутий з `date`.
5.	Видалити стовпці `quantity` та `price`.
6.	Замінити пропущені значення в `date` на найближчу попередню дату.
7.	Створити зведену таблицю, де в рядках буде `month`, у стовпцях – `store_id`, а в комірках – кількість унікальних товарів (`product_id`), проданих в кожному магазині кожного місяця.

**Контрольне запитання:** Як заповнити пропущені значення в DataFrame, використовуючи значення з попередніх або наступних рядків?

---

### Варіант 7
1.	Вибрати рядки, де `store_id` дорівнює 1, а `product_id` не дорівнює 3.
2.	Відсортувати дані за спаданням `date`, а потім за зростанням `order_id`.
3.	Згрупувати дані за `store_id` і `month` (витягнути з `date`) та обчислити медіану `quantity` для кожного магазину в кожному місяці.
4.	Додати стовпець `is_weekend`, який містить `True`, якщо `date` припадає на вихідний день (субота або неділя), інакше `False`.
5.	Видалити рядки, де `total_amount` (добуток `quantity` на `price`) менше середнього значення `total_amount` по всьому DataFrame.
6.	Заповнити пропущені значення в `product_id` випадковими значеннями з існуючого набору унікальних `product_id`.
7.	Створити зведену таблицю, де в рядках буде `store_id`, а в стовпцях – `is_weekend`, а в комірках – загальна кількість (`quantity`) проданих товарів у вихідні та будні дні для кожного магазину.

**Контрольне запитання:** Як обчислити квантилі (наприклад, квартилі) для числового стовпця в DataFrame?

---

### Варіант 8
1.	Вибрати рядки, де `date` є останнім днем місяця.
2.	Відсортувати дані за `price` у порядку зростання, а потім за `quantity` у порядку спадання.
3.	Згрупувати дані за `product_id` та обчислити коефіцієнт варіації (відношення стандартного відхилення до середнього) для `price` кожного товару.
4.	Додати стовпець `year`, який містить рік, витягнутий з `date`.
5.	Видалити рядки, де `quantity` дорівнює 1.
6.	Замінити пропущені значення в `total_amount` (добуток `quantity` на `price`) на середнє значення `total_amount` для відповідного `store_id` та `product_id`.
7.	Створити зведену таблицю, де в рядках буде `year`, а в стовпцях – `store_id`, а в комірках – максимальна виручка (`total_amount`) в кожному році для кожного магазину.

**Контрольне запитання:** Як визначити, чи є дата останнім днем місяця в pandas?

---

### Варіант 9
1.	Вибрати рядки, де `order_id` є парним числом.
2.	Відсортувати дані за `store_id` та `product_id` у порядку зростання.
3.	Згрупувати дані за `date` і обчислити кількість замовлень (`order_id`), в яких було продано більше одного унікального товару (`product_id`).
4.	Додати стовпець `avg_price`, який містить середню ціну товару (`product_id`) по всьому DataFrame.
5.	Видалити стовпці `date` та `order_id`.
6.	Заповнити пропущені значення в `price` лінійною інтерполяцією.
7.	Створити зведену таблицю, де в рядках буде `store_id`, у стовпцях – `product_id`, а в комірках – кількість замовлень, в яких цей товар був проданий в даному магазині.

**Контрольне запитання:** Як виконати лінійну інтерполяцію для заповнення пропущених значень у DataFrame?

---

### Варіант 10
1.	Вибрати рядки, де `price` вище середнього значення для відповідного `product_id`.
2.	Відсортувати дані за `date` у порядку зростання, а потім за `total_amount` (добуток `quantity` на `price`) у порядку спадання.
3.	Згрупувати дані за `store_id` та `date` і обчислити частку кожного товару (`product_id`) у загальній кількості (`quantity`) проданих товарів в цей день в даному магазині.
4.	Додати стовпець `avg_price`, який містить середню ціну товару (`product_id`) по всьому DataFrame.
5.	Видалити рядки, де `store_id` дорівнює 2, а `product_id` дорівнює 5.
6.	Заповнити пропущені значення в `quantity` медіаною `quantity` для відповідного `store_id` та `date`.
7.	Створити зведену таблицю, де в рядках буде `store_id`, у стовпцях – `product_id`, а в комірках – кількість замовлень, в яких цей товар був проданий в даному магазині.

**Контрольне запитання:** Як обчислити частку кожного елемента групи в загальній сумі значень для цієї групи в pandas?

---

### Варіант 11
1.	Вибрати рядки, де кількість `quantity` більша за 3.
2.	Відсортувати дані за датою `date` у порядку зростання.
3.	Згрупувати дані за `store_id` та обчислити середню кількість `quantity` товарів у замовленні для кожного магазину.
4.	Додати стовпець `quarter`, який містить номер кварталу на основі дати `date`.
5.	Видалити стовпці `order_id` та `product_id`.
6.	Замінити пропущені значення в `price` на середнє значення.
7.	Створити зведену таблицю, де в рядках буде `store_id`, у стовпцях – квартали (`quarter`), а в комірках – сума `price` для кожного магазину та кварталу.

**Контрольне запитання:** Як витягнути номер кварталу з дати в pandas?

---

### Варіант 12
1.	Вибрати рядки з 10 по 20 (включно).
2.	Відсортувати дані за `price` у порядку спадання.
3.	Згрупувати дані за `product_id` та обчислити максимальну кількість `quantity` в одному замовленні для кожного товару.
4.	Додати стовпець `total_amount`, що дорівнює добутку `quantity` та `price`.
5.	Видалити дублікати за стовпцем `order_id`, залишаючи останній рядок з кожним `order_id`.
6.	Заповнити пропущені значення в `store_id` попереднім значенням (forward fill).
7.	Створити зведену таблицю, де в рядках буде `product_id`, у стовпцях – `store_id`, а в комірках – мінімальна `price` для кожного товару та магазину.

**Контрольне запитання:** Як видалити дублікати рядків у DataFrame на основі значень у певному стовпці?

---

### Варіант 13
1.	Вибрати рядки, де `store_id` дорівнює 1 або 3.
2.	Відсортувати дані за `store_id` у порядку зростання, а потім за `date` у порядку спадання.
3.	Згрупувати дані за `date` та обчислити кількість унікальних `product_id`, проданих у цей день.
4.	Додати стовпець `sale_rank`, який містить ранг кожного замовлення за `total_amount` (добуток `quantity` на `price`) в межах кожного `store_id`.
5.	Видалити рядки, де `quantity` дорівнює 1 або 2.
6.	Замінити пропущені значення в `total_amount` на 0.
7.	Створити зведену таблицю, де в рядках буде `date`, а в стовпцях – `store_id`, а в комірках – середнє значення `sale_rank` для кожного дня та магазину.

**Контрольне запитання:** Як створити рейтинг (ранжувати) рядків у DataFrame на основі значень у певному стовпці?

---

### Варіант 14
1.	Вибрати рядки, де день тижня (витягнутий з `date`) є понеділком.
2.	Відсортувати дані за зростанням `price`, а потім за спаданням `quantity`.
3.	Згрупувати дані за `store_id` та `product_id` і обчислити суму `quantity` та середнє значення `price` для кожного товару в кожному магазині.
4.	Додати стовпець `weekday`, який містить назву дня тижня, витягнуту з `date`.
5.	Видалити стовпці `date` та `order_id`.
6.	Замінити пропущені значення в `quantity` на мінімальне значення `quantity` для відповідного `store_id`.
7.	Створити зведену таблицю, де в рядках буде `store_id`, у стовпцях – `weekday`, а в комірках – максимальна `price` для кожного магазину та дня тижня.

**Контрольне запитання:** Як отримати назву дня тижня з об’єкта datetime в pandas?

---

### Варіант 15
1.	Вибрати рядки, де `product_id` належить до списку [1, 3, 5, 7, 9].
2.	Відсортувати дані за `quantity` у порядку зростання, а потім за `price` у порядку спадання.
3.	Згрупувати дані за `month` (витягнути з `date`) та обчислити середню кількість унікальних `store_id`, в яких продавався кожен `product_id`.
4.	Додати стовпець `price_diff`, який містить різницю між `price` та середньою ціною (`price`) для відповідного `product_id`.
5.	Видалити рядки, де `price_diff` більше стандартного відхилення `price` для відповідного `product_id`.
6.	Замінити пропущені значення в `store_id` на 0.
7.	Створити зведену таблицю, де в рядках буде `month`, у стовпцях – `product_id`, а в комірках – середнє значення `price_diff` для кожного місяця та товару.

**Контрольне запитання:** Як обчислити різницю між значенням у кожному рядку та середнім значенням по групі в pandas?

---

### Варіант 16
1.	Вибрати рядки, де `store_id` не дорівнює 2.
2.	Відсортувати дані за `order_id` у порядку зростання.
3.	Згрупувати дані за `product_id` і `store_id` та обчислити кількість днів, коли цей товар продавався в цьому магазині.
4.	Додати стовпець `running_total`, який містить накопичувальну суму `quantity` в межах кожного `store_id`, відсортовану за `date`.
5.	Видалити стовпець `price`.
6.	Замінити пропущені значення в `date` на попереднє значення (forward fill).
7.	Створити зведену таблицю, де в рядках буде `store_id`, у стовпцях – `product_id`, а в комірках – максимальне значення `running_total` для кожного магазину та товару.

**Контрольне запитання:** Як обчислити накопичувальну суму значень у стовпці в межах кожної групи?

---

### Варіант 17
1.	Вибрати рядки, де `date` припадає на першу половину місяця (з 1 по 15 число).
2.	Відсортувати дані за спаданням `total_amount` (добуток `quantity` на `price`), а потім за зростанням `date`.
3.	Згрупувати дані за `store_id` та обчислити середньозважене значення `price` для кожного магазину, де вагами є `quantity`.
4.	Додати стовпець `scaled_quantity`, який містить масштабовані значення `quantity` в діапазоні від 0 до 1 в межах кожного `store_id`.
5.	Видалити рядки, де `scaled_quantity` менше 0.5.
6.	Замінити пропущені значення в `total_amount` на медіану `total_amount` для відповідного `product_id`.
7.	Створити зведену таблицю, де в рядках буде `store_id`, а в стовпцях – місяці (витягнуті з `date`), а в комірках – сума `scaled_quantity` для кожного магазину та місяця.

**Контрольне запитання:** Як масштабувати значення в стовпці до певного діапазону (наприклад, від 0 до 1) в pandas?

---

### Варіант 18
1.	Вибрати рядки, де `quantity` парне число.
2.	Відсортувати дані за `store_id` у порядку зростання, а потім за `product_id` у порядку спадання.
3.	Згрупувати дані за `date` та обчислити медіану `price` для кожного дня, а також кількість унікальних `store_id`, які зробили замовлення в цей день.
4.	Додати стовпець `price_rank`, який містить ранг кожного товару за середньою ціною (`price`) в межах кожного `store_id`.
5.	Видалити всі стовпці, крім `store_id`, `product_id`, `price_rank` та `date`.
6.	Замінити пропущені значення в `price_rank` на максимальне значення `price_rank` + 1.
7.	Створити зведену таблицю, де в рядках буде `date`, а в стовпцях – `store_id`, а в комірках – мінімальне значення `price_rank` для кожного дня та магазину.

**Контрольне запитання:** Як обчислити ранг елементів у стовпці в межах кожної групи в pandas?

---

### Варіант 19
1.	Вибрати рядки, де `price` менше середнього значення `price` по всьому DataFrame, але `quantity` більше середнього значення `quantity` по всьому DataFrame.
2.	Відсортувати дані за `date` у порядку спадання, а потім за `total_amount` (добуток `quantity` на `price`) у порядку зростання.
3.	Згрупувати дані за `store_id` та `product_id` і обчислити кількість замовлень (`order_id`), де `quantity` була більшою за середню `quantity` для цього товару в цьому магазині.
4.	Додати стовпець `high_demand`, який містить `True`, якщо `quantity` більше 75-го перцентиля `quantity` для відповідного `product_id`, інакше `False`.
5.	Видалити рядки, де `high_demand` дорівнює `False`.
6.	Заповнити пропущені значення в `total_amount` середнім значенням `total_amount` по всьому DataFrame.
7.	Створити зведену таблицю, де в рядках буде `store_id`, у стовпцях – `product_id`, а в комірках – кількість днів, коли цей товар був у високому попиті (`high_demand`) в цьому магазині.

**Контрольне запитання:** Як обчислити перцентилі (наприклад, 75-й перцентиль) для числового стовпця в DataFrame?

---

### Варіант 20
1.	Вибрати рядки, де `order_id` кратний 5.
2.	Відсортувати дані за `total_amount` (добуток `quantity` на `price`) у порядку спадання.
3.	Згрупувати дані за `month` (витягнути з `date`) та `store_id` і обчислити середнє значення `total_amount` для кожного місяця та магазину.
4.	Додати стовпець `shifted_quantity`, який містить значення `quantity` зі зміщенням на один рядок вперед в межах кожного `store_id`.
5.	Видалити стовпці `order_id`, `date` та `product_id`.
6.	Замінити пропущені значення в `shifted_quantity` на 0.
7.	Створити зведену таблицю, де в рядках буде `store_id`, а в стовпцях – `month`, а в комірках – кореляція між `quantity` та `shifted_quantity` для кожного магазину та місяця.

**Контрольне запитання:** Як створити новий стовпець, що містить значення з іншого стовпця зі зміщенням на певну кількість рядків?

---

### Варіант 21
1.	Вибрати рядки, де `store_id` дорівнює 1, а `product_id` не входить до списку [2, 4, 6].
2.	Відсортувати дані за `date` у порядку зростання, а потім за `order_id` у порядку спадання.
3.	Згрупувати дані за `date` та обчислити середньозважене значення `price` для кожного дня, де вагами є `quantity`.
4.	Додати стовпець `above_avg_total`, який містить `True`, якщо `total_amount` (добуток `quantity` на `price`) вище середнього `total_amount` для відповідного `store_id` та `product_id`, інакше `False`.
5.	Видалити рядки, де `above_avg_total` дорівнює `False`.
6.	Замінити пропущені значення в `price` на попереднє не пропущене значення в межах кожного `store_id`.
7.	Створити зведену таблицю, де в рядках буде `date`, а в стовпцях – `product_id`, а в комірках – кількість замовлень, де `above_avg_total` дорівнює `True`, для кожного дня та товару.

**Контрольне запитання:** Як обчислити середньозважене значення в pandas, де значення одного стовпця зважуються значеннями іншого стовпця?

---

### Варіант 22
1.	Вибрати рядки, де `date` є останнім робочим днем місяця (п’ятниця, якщо не випадає на вихідний).
2.	Відсортувати дані за `product_id` у порядку зростання, а потім за `total_amount` (добуток `quantity` на `price`) у порядку спадання.
3.	Згрупувати дані за `store_id` та `month` (витягнути з `date`) і обчислити коефіцієнт варіації (відношення стандартного відхилення до середнього) для `total_amount` в кожному місяці для кожного магазину.
4.	Додати стовпець `price_to_avg_ratio`, який містить відношення `price` до середньої ціни (`price`) для відповідного `product_id`.
5.	Видалити стовпці `date`, `quantity`, `price`.
6.	Заповнити пропущені значення в `total_amount` мінімальним значенням `total_amount` в усьому DataFrame.
7.	Створити зведену таблицю, де в рядках буде `store_id`, у стовпцях – `month`, а в комірках – середнє значення `price_to_avg_ratio` для кожного магазину та місяця.

**Контрольне запитання:** Як визначити, чи є дата останнім робочим днем місяця в pandas?

---

### Варіант 23
1.	Вибрати рядки, де `quantity` більше медіани `quantity` для відповідного `store_id`.
2.	Відсортувати дані за спаданням `date`, а потім за зростанням `store_id`.
3.	Згрупувати дані за `product_id` та обчислити середню кількість днів між послідовними покупками цього товару.
4.	Додати стовпець `prev_order_date`, який містить дату попереднього замовлення в межах кожного `store_id`, відсортованого за `date`.
5.	Видалити рядки, де `date` дорівнює `prev_order_date`.
6.	Замінити пропущені значення в `prev_order_date` на мінімальну дату в усьому DataFrame.
7.	Створити зведену таблицю, де в рядках буде `product_id`, а в стовпцях – `store_id`, а в комірках – середня різниця в днях між `date` та `prev_order_date` для кожного товару та магазину.

**Контрольне запитання:** Як обчислити різницю між послідовними значеннями в стовпці (наприклад, датами) в pandas?

---

### Варіант 24
1.	Вибрати рядки, де `store_id` дорівнює 2 або 3, а `price` вище 75-го перцентиля `price` по всьому DataFrame.
2.	Відсортувати дані за `quantity` у порядку зростання, а потім за `date` у порядку спадання.
3.	Згрупувати дані за `date` та `store_id` і обчислити частку кожного `product_id` у загальній кількості (`quantity`) проданих товарів в цей день в цьому магазині.
4.	Додати стовпець `month_year`, який містить комбінацію місяця та року, витягнуту з `date`.
5.	Видалити рядки, де частка `product_id`, розрахована в п.3, менше за 0.1.
6.	Заповнити пропущені значення в `price` середнім значенням `price` для відповідного `month_year` та `store_id`.
7.	Створити зведену таблицю, де в рядках буде `month_year`, а в стовпцях – `store_id`, а в комірках – максимальна частка `product_id` серед усіх товарів, проданих в цей період в цьому магазині.

**Контрольне запитання:** Як скомбінувати значення з декількох стовпців в один, розділивши їх певним символом (наприклад, пробілом або дефісом)?

---

### Варіант 25
1.	Вибрати рядки, де `order_id` є простим числом.
2.	Відсортувати дані за `price` у порядку зростання, а потім за `quantity` у порядку спадання.
3.	Згрупувати дані за `store_id` та `month` (витягнути з `date`) і обчислити 90-й перцентиль `total_amount` (добуток `quantity` на `price`) для кожного місяця та магазину.
4.	Додати стовпець `total_rank`, який містить ранг кожного замовлення за `total_amount` в межах усього DataFrame, відсортованого за спаданням `total_amount`.
5.	Видалити стовпці `date` та `order_id`.
6.	Замінити пропущені значення в `total_rank` на -1.
7.	Створити зведену таблицю, де в рядках буде `store_id`, а в стовпцях – `month`, а в комірках – середнє значення `total_rank` для кожного магазину та місяця.

**Контрольне запитання:** Як перевірити, чи є число простим, в Python? (Можна використати ефективний алгоритм або бібліотечну функцію).

---

### Варіант 26
1.	Вибрати рядки, де `date` припадає на перші 5 днів кожного місяця.
2.	Відсортувати дані за `store_id` у порядку зростання, а потім за `date` у порядку зростання.
3.	Згрупувати дані за `product_id` та обчислити середню кількість (`quantity`) цього товару, що продавалася в магазинах, де він взагалі продавався.
4.	Додати стовпець `avg_store_quantity`, який містить середню кількість (`quantity`) цього товару (`product_id`), що продається в кожному окремому магазині (`store_id`) протягом усього періоду.
5.	Видалити рядки, де `quantity` менше `avg_store_quantity`.
6.	Заповнити пропущені значення в `avg_store_quantity` нулем.
7.	Створити зведену таблицю, де в рядках буде `product_id`, а в стовпцях – `store_id`, а в комірках – відношення `quantity` до `avg_store_quantity` для кожного товару та магазину (якщо `avg_store_quantity` не дорівнює нулю).

**Контрольне запитання:** Як обчислити середнє значення в стовпці, ігноруючи нульові значення, в pandas?

---

### Варіант 27
1.	Вибрати рядки, де `quantity` більше 2 та `price` менше середнього значення `price` для відповідного `store_id`.
2.	Відсортувати дані за `date` у порядку спадання.
3.	Згрупувати дані за `store_id` та `month` (витягнути з `date`) і обчислити суму `total_amount` (добуток `quantity` на `price`) для кожного магазину в кожному місяці.
4.	Додати стовпець `lagged_price`, який містить значення `price` з попереднього рядка в межах кожного `product_id`, відсортованого за `date`.
5.	Видалити стовпці `order_id`, `quantity` та `lagged_price`.
6.	Заповнити пропущені значення в `price` середнім значенням `price` по всьому DataFrame.
7.	Створити зведену таблицю, де в рядках буде `store_id`, у стовпцях – `month`, а в комірках – максимальне значення `price` серед товарів (`product_id`), для яких `total_amount` в цьому місяці був вище медіани `total_amount` по всьому DataFrame за цей місяць.

**Контрольне запитання:** Як отримати доступ до значення з попереднього або наступного рядка в pandas DataFrame?

---

### Варіант 28
1.	Вибрати рядки, де `date` припадає на робочі дні (з понеділка по п’ятницю).
2.	Відсортувати дані за `store_id` та `product_id` у порядку зростання.
3.	Згрупувати дані за `product_id` та обчислити середню різницю між максимальною та мінімальною ціною (`price`) цього товару в різних магазинах (`store_id`).
4.	Додати стовпець `price_range`, який містить різницю між максимальною та мінімальною `price` для кожного `product_id` в межах кожного `store_id`.
5.	Видалити рядки, де `price_range` дорівнює нулю.
6.	Заповнити пропущені значення в `price_range` середнім значенням `price_range` для відповідного `product_id`.
7.	Створити зведену таблицю, де в рядках буде `product_id`, а в стовпцях – `store_id`, а в комірках – стандатрне відхилення `price_range` для кожного товару та магазину.

**Контрольне запитання:** Як обчислити різницю між максимальним та мінімальним значенням у стовпці для кожної групи в pandas?

---

### Варіант 29
1.	Вибрати рядки, де `quantity` є непарним числом і `store_id` дорівнює 3.
2.	Відсортувати дані за `date` у порядку зростання, а потім за `price` у порядку спадання.
3.	Згрупувати дані за `month_year` (комбінація місяця та року з `date`) та обчислити загальну кількість (`quantity`) проданих товарів та кількість унікальних товарів (`product_id`) за цей період.
4.	Додати стовпець `is_expensive`, який містить `True`, якщо `price` вище 75-го перцентиля `price` по всьому DataFrame, інакше `False`.
5.	Видалити рядки, де `is_expensive` дорівнює `True`.
6.	Замінити пропущені значення в `month_year` на 'Невідомо'.
7.	Створити зведену таблицю, де в рядках буде `month_year`, а в стовпцях – `is_expensive`, а в комірках – середнє значення `quantity` для кожного періоду та категорії ціни.

**Контрольне запитання:** Як створити новий стовпець, що містить комбінацію значень з декількох інших стовпців, в pandas?

---

### Варіант 30
1.	Вибрати рядки, де `total_amount` (добуток `quantity` на `price`) більше середнього `total_amount` для відповідного `product_id`.
2.	Відсортувати дані за `product_id` у порядку зростання, а потім за `total_amount` у порядку спадання.
3.	Згрупувати дані за `store_id` та `product_id` і обчислити, скільки разів `total_amount` перевищував середнє значення `total_amount` для цього `product_id` в цьому `store_id`.
4.	Додати стовпець `quantity_ratio`, який містить відношення `quantity` до середнього значення `quantity` для відповідного `store_id` та `date`.
5.	Видалити всі стовпці, крім `store_id`, `product_id`, `total_amount`, `quantity_ratio` та `date`.
6.	Заповнити пропущені значення в `quantity_ratio` одиницею.
7.	Створити зведену таблицю, де в рядках буде `store_id`, а в стовпцях – `product_id`, а в комірках – максимальне значення `quantity_ratio` для кожного магазину та товару.

**Контрольне запитання:** Як порахувати кількість випадків, коли значення в стовпці відповідає певній умові, для кожної групи в pandas?
